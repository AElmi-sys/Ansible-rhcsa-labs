
---
- name: Get hostname and ip addr
  hosts: all
  become: yes

  tasks:
    - name: Display hostname
      debug:
        msg: "Host name is {{ ansible_hostname }}"

    - name: Display IP address
      debug:
        msg: "IP is {{ ansible_default_ipv4.address }}" 


