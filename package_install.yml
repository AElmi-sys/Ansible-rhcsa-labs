
---
- name: Install and remove a package
  hosts: all
  become: yes

  tasks:
    - name: Install a package  
      ansible.builtin.dnf:
        name: httpd
        state: present

    - name: Start and enable  httpd
      ansible.builtin.service:
        name: httpd
        enabled: yes
        state: started

